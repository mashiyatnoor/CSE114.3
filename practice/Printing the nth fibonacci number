#include <stdio.h>
void main() {
    int num1 = 0, num2 = 1, n, fib;
    printf("Enter the value of n:");
    scanf("%d", &n);

    for(int i=1; i<=n-2; i++) {
        fib = num1 + num2;
        num1 = num2;
        num2 = fib;
    }
    printf("\n %dth Fibonacci Number %d",n,fib);
}
